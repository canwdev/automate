<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('_head'); -%>
  <title>日志输出：<%= fileName %></title>
</head>

<body>
  <p class="tip">日志输出 <span id='sec'>5</span>s 刷新一次【<a href="/logs/<%= fileName %>?raw=true" class="cur-help"
      title="日志源文件不会自动刷新">日志源文件</a> |
    <a href="/logs">日志列表</a>】</p>
  <div class="log-content">
    <img class="loading" src="/loadimg.gif">
    <pre id="log"><%= content %></pre>
  </div>
  <script>
    window.onload = function () {
      requestLog()
      window.setInterval(requestLog, 5000)
    }

    var logEl = document.getElementById('log')
    function requestLog() {
      var xhr = new XMLHttpRequest()
      xhr.open("GET", "/logs/<%= fileName %>?raw=true&tail=true", true, null, null)
      xhr.send()
      xhr.onload = function () {
        // console.log(`Loaded: ${xhr.status} ${xhr.response}`)
        logEl.innerHTML = xhr.response
      }
      xhr.onerror = function () {
        console.error(`Network Error`)
      }
    }
  </script>
</body>

</html>